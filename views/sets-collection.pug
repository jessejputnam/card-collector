extends layout 

block content 
  div.set-page-wrapper
    h1 Set Completion 

    div.set-results-container
      each set in list_sets 
        hr(style="margin-top:15px")
        div.set-container
          h3= set[0].meta.set.name 
            span(style="font-size:.8rem;color:gray;font-weight:normal")  #{set.length}/#{set[0].meta.set.totalPrint}
            span(style="font-size:.5rem;color:gray;margin-left:15px")= set[0].meta.set.releaseDate.slice(0, 4)
          div.set-card-list
            each card in set 
              li(style='list-style-type:hiragana;font-size:.6rem')  
                a.set-link(href=`/collection/${card._id}` style=`font-size:.9rem;font-weight:normal;color:${card.meta.rarity.grade < 3 ? "#9e2a2b" : "inherit"}`)= card.pokemon.name + " "
                  span(style="font-size:.7rem")=  card.meta.set.number + "/" + card.meta.set.totalPrint